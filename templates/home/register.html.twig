{%  extends 'base.html.twig' %}

{% block title "S'inscrire - Le quai antique" %}
{% block body %}
<div class="jumbotron text-center">
        <h1>S'inscrire</h1>
</div>
<div class="container">
{{ form_start(register_form)}}
        {{ form_row(register_form.email)}}
        {{ form_row(register_form.password)}}
        {{ form_row(register_form.confirmPassword)}}
        {{ form_row(register_form.convives)}}
        {{ form_row(register_form.allergies)}}
                <input type="submit" value="Envoyer">
{{ form_end(register_form)}}
</div>
{% endblock %}
