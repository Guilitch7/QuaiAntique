{%  extends 'base.html.twig' %}

{% block stylesheet %}
<head>
<link rel="stylesheet" href="{{ asset('styles/plat-style.css') }}">
</head>
{% endblock %}

{% block title "Nos viandes - Le quai antique" %}
{% block body %}
<div class="jumbotron text-center">
        <h1>Nos Viandes</h1>
</div>
<main class="container">
    <div class="container">
        <div class="row">
            {% for viande in viandes %}
            <div class="col-4 text-center">
                <h2>{{ viande.DISHESname }}</h2>
                    <div>
                        <img class="img-fluid" src="{{asset(viande.DISHESphoto)}}" alt="{{ viande.DISHESname }}">
                        <p class="description">{{ viande.DISHESdescription }}</p>
                        <p class="price">{{ viande.DISHESprice }} €</p>
                        {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path ('delete-dish', {'id' : viande.DISHESid}) }}" class="btn btn-danger">Supprimer</a>
                        <a href="{{ path ('edit-dish', {'id' : viande.DISHESid}) }}" class="btn btn-warning">Modifier</a>
                        {% endif %}
                    </div>
            </div>
            {% endfor %}
        </div>
    </div>
</main>
{% endblock %}