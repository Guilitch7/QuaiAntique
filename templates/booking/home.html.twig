{%  extends 'base.html.twig' %}
{% block stylesheets %}
<head>
<link rel="stylesheet" href="{{ asset('styles/book-style.css') }}">
</head>
{% endblock %}
{% block title "Réserver - Le quai antique" %}
{% block body %}
<main>
<div class="jumbotron text-center title">
        <h1>Réserver</h1>
        <p>Afin de pouvoir vous accueillir dans les meilleures conditions, merci de procéder à une réservation en ligne</p>
</div>
<div class="container">
        {{ form_start(booking_form)}}
            <div id="form1">
              <img src="{{asset('uploads/1-square.svg')}}" width="60" alt="icone" class="mb-3 text-center">
                {#{ form_row(booking_form.BOOKINGStimeslot)|date('H:i')}#}
                {#{ form_row(booking_form.OPENINGDAYSday)}#}
                {{ form_row(booking_form.BookSlotDate)}}
                {#{ form_row(booking_form.OpenLunch)}#}
                {{ form_row(booking_form.slotLunch)}}
                {{ form_row(booking_form.BookSlotCovers )}}
                <button type="button" id="btn1" class="mb-3">Suite</button>
            </div>
            <p id="alert" class="text-danger text-center fw-bold">Il n'est pas possible de réserver dans l'heure précédant la fermeture ou nous sommes fermés à cette heure, reportez-vous aux horaires d'ouverture</p>
            <p id="alert2" class="text-danger text-center fw-bold">Nous n'avons plus de disponibilités pour cette date, désolé</p>
            <div id="form2">
              <img src="{{asset('uploads/2-square.svg')}}" width="60" alt="icone" class="mb-3">
                {{ form_row(booking_form.BookSlotUser)}}
                
                {{ form_row(booking_form.BookSlotAllergies)}}
                <button type="button" id="btn2" class="mb-3">Retour</button>
                <div>
                  <input type="submit" id="submit" value="Envoyer" class="btn btn-Info mb-3">
                </div>
            </div>
        {{ form_end(booking_form)}}
</div>
</main>
{% endblock %}
{% block javascript %}

<script src="{{ asset('scripts/booking-script.js') }}"></script>

{% endblock %}